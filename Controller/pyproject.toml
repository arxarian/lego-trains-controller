[project]
name = "TrainsController"
version = "0.1.0"
dependencies = [
    "PySide6",
    "pytest",
    "bleak",
    "networkx",
    "pydot",
    "qasync",
    "webcolors"
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
