# Stale / sample HSV notes from older calibrations — not used by decodeHSV.
# Re-measure with tests.py (print hsv) under track lighting if thresholds drift.
#
# Typical targets (illustrative only):
#   white  — high V, low S (e.g. v>=75, s<=35); hue unreliable
#   red    — h near 0/360, high S
#   yellow — h ~47, high S/V
#   green  — h ~100-160
#   blue   — h ~190-260, moderate S (NOT the same as green)
#   gray   — mid V, very low S
#   black  — low V
